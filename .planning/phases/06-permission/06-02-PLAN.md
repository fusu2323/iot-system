---
phase: 06
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/example/iot/controller/LogController.java
autonomous: true
requirements:
  - AUTH-06
  - PERMISSION-01
  - PERMISSION-03

must_haves:
  truths:
    - "Non-admin users receive HTTP 403 when accessing /api/logs endpoints"
    - "Admin users can access /api/logs endpoints without restriction"
  artifacts:
    - path: "backend/src/main/java/com/example/iot/controller/LogController.java"
      provides: "Admin-only access to all log endpoints"
      contains: "@PreAuthorize(\"hasRole('ADMIN')\")"
  key_links:
    - from: "LogController.java"
      to: "SecurityConfig.java"
      via: "@EnableMethodSecurity"
      pattern: "@EnableMethodSecurity enables @PreAuthorize"
---

<objective>
Add @PreAuthorize("hasRole('ADMIN')") to all LogController endpoints so that non-admin users receive HTTP 403 Forbidden. No service layer changes needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@backend/src/main/java/com/example/iot/controller/LogController.java
@backend/src/main/java/com/example/iot/config/SecurityConfig.java

**Key patterns from existing code:**
- SecurityConfig has `@EnableMethodSecurity` on line 26 — @PreAuthorize will work
- JwtUserDetailsService creates authorities as `"ROLE_" + user.getRole()` (line 34 in JwtUserDetailsService.java)
- GlobalExceptionHandler already maps AccessDeniedException to FORBIDDEN (ResultCode.FORBIDDEN)
- LogController has 4 endpoints: list, getLogById, countByOperation, countByDay — all should be ADMIN-only
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @PreAuthorize to LogController</name>
  <files>backend/src/main/java/com/example/iot/controller/LogController.java</files>
  <read_first>
backend/src/main/java/com/example/iot/controller/LogController.java
backend/src/main/java/com/example/iot/config/SecurityConfig.java
  </read_first>
  <acceptance_criteria>
    <criteria>File contains `import org.springframework.security.access.prepost.PreAuthorize;`</criteria>
    <criteria>Class-level javadoc or import section includes swagger annotation `@Tag(name = "日志管理"`</criteria>
    <criteria>Method `list()` has `@PreAuthorize("hasRole('ADMIN')")` annotation</criteria>
    <criteria>Method `getLogById()` has `@PreAuthorize("hasRole('ADMIN')")` annotation</criteria>
    <criteria>Method `countByOperation()` has `@PreAuthorize("hasRole('ADMIN')")` annotation</criteria>
    <criteria>Method `countByDay()` has `@PreAuthorize("hasRole('ADMIN')")` annotation</criteria>
  </acceptance_criteria>
  <action>
Modify `backend/src/main/java/com/example/iot/controller/LogController.java`:

1. Add import after existing imports:
```java
import org.springframework.security.access.prepost.PreAuthorize;
```

2. Add `@PreAuthorize("hasRole('ADMIN')")` annotation to each of the 4 endpoints:
- `@GetMapping` on `list()` method
- `@GetMapping("/{id}")` on `getLogById()` method
- `@GetMapping("/stats/operation")` on `countByOperation()` method
- `@GetMapping("/stats/daily")` on `countByDay()` method

Example pattern for each method:
```java
@GetMapping
@PreAuthorize("hasRole('ADMIN')")
@Operation(summary = "查询操作日志列表")
public Result<IPage<OperationLogVO>> list(...) {
```

Do NOT modify any method implementation — only add the @PreAuthorize annotation before each endpoint method.
  </action>
  <verify>
    <automated>grep -c 'PreAuthorize.*hasRole' backend/src/main/java/com/example/iot/controller/LogController.java</automated>
  </verify>
  <done>All 4 LogController endpoints have @PreAuthorize("hasRole('ADMIN')"); non-admin requests receive 403</done>
</task>

</tasks>

<verification>
- All 4 LogController endpoints annotated with @PreAuthorize("hasRole('ADMIN')")
- No method implementations changed
- Import added for PreAuthorize
</verification>

<success_criteria>
Non-admin users (role=USER) accessing any /api/logs endpoint receive HTTP 403 Forbidden. Admin users (role=ADMIN) can access all log endpoints normally. The GlobalExceptionHandler maps AccessDeniedException to ResultCode.FORBIDDEN automatically.
</success_criteria>

<output>
After completion, create `.planning/phases/06-permission/06-02-SUMMARY.md`
</output>
