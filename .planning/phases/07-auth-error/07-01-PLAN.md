---
phase: 07-auth-error
plan: '01'
type: execute
wave: '1'
depends_on: []
files_modified:
  - backend/src/main/resources/db/migration/V1.3__add_user_email.sql
  - backend/src/main/java/com/example/iot/entity/User.java
  - backend/src/main/java/com/example/iot/dto/UserRegisterDTO.java
  - backend/src/main/java/com/example/iot/mapper/UserMapper.java
  - backend/src/main/java/com/example/iot/common/result/ResultCode.java
  - backend/src/main/java/com/example/iot/service/impl/UserServiceImpl.java
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
must_haves:
  truths:
    - "User sees '用户名已存在，请尝试其他用户名' when registering with duplicate username"
    - "User sees '该邮箱已被注册' when registering with duplicate email"
    - "Email is stored in database when provided during registration"
  artifacts:
    - path: backend/src/main/resources/db/migration/V1.3__add_user_email.sql
      provides: Adds email column with UNIQUE constraint to user table
    - path: backend/src/main/java/com/example/iot/entity/User.java
      provides: User entity with email field
      contains: private String email
    - path: backend/src/main/java/com/example/iot/dto/UserRegisterDTO.java
      provides: Registration DTO with optional email field
      contains: private String email
    - path: backend/src/main/java/com/example/iot/mapper/UserMapper.java
      provides: selectByEmail query method
      contains: selectByEmail
    - path: backend/src/main/java/com/example/iot/common/result/ResultCode.java
      provides: EMAIL_ALREADY_EXISTS error code
      contains: EMAIL_ALREADY_EXISTS
    - path: backend/src/main/java/com/example/iot/service/impl/UserServiceImpl.java
      provides: register() with email uniqueness check and custom error messages
      contains: selectByEmail
  key_links:
    - from: UserServiceImpl.register()
      to: UserMapper.selectByEmail()
      via: email uniqueness check call
      pattern: selectByEmail(dto.getEmail())
    - from: UserServiceImpl.register()
      to: User entity
      via: user.setEmail(dto.getEmail())
      pattern: user.setEmail
    - from: V1.3 migration
      to: User entity
      via: DB schema enabling email field
      pattern: ADD COLUMN email
---

<objective>
Implement clear user-facing error messages for duplicate username (AUTH-01) and duplicate email (AUTH-02) during registration. Add email field support to the user entity, DTO, mapper, and database schema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/src/main/java/com/example/iot/entity/User.java
@backend/src/main/java/com/example/iot/dto/UserRegisterDTO.java
@backend/src/main/java/com/example/iot/mapper/UserMapper.java
@backend/src/main/java/com/example/iot/common/result/ResultCode.java
@backend/src/main/java/com/example/iot/common/exception/BusinessException.java
@backend/src/main/java/com/example/iot/service/impl/UserServiceImpl.java
@backend/src/main/resources/db/migration/V1.2__add_log_statistics_tables.sql

## Locked Decisions (per 07-CONTEXT.md)

- **D-01:** Username conflict message = `"用户名已存在，请尝试其他用户名"`
- **D-02:** Email conflict message = `"该邮箱已被注册"`
- **D-03:** Email field is optional during registration
- **D-04:** Email UNIQUE KEY constraint in DB (multiple NULLs allowed)
- **D-05:** Use BusinessException + ResultCode; username conflict reuses USER_ALREADY_EXISTS with message override; email conflict uses new EMAIL_ALREADY_EXISTS code
- **D-06:** Flyway migration V1.3__add_user_email.sql

## Existing Patterns

- UserMapper uses `@Select` annotation with `AND deleted = 0` filter (see selectByUsername at line 18)
- BusinessException(Integer code, String message) constructor for message override
- snake_case DB columns map to camelCase Java fields
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Flyway V1.3 migration for email column</name>
  <files>backend/src/main/resources/db/migration/V1.3__add_user_email.sql</files>
  <read_first>
    - backend/src/main/resources/db/migration/V1.2__add_log_statistics_tables.sql (migration pattern reference)
  </read_first>
  <action>
    Create file `V1.3__add_user_email.sql` in `backend/src/main/resources/db/migration/` with content:

    ```sql
    -- Phase 7: Add email column to user table
    -- Supports AUTH-01 and AUTH-02

    ALTER TABLE user ADD COLUMN email VARCHAR(100) DEFAULT NULL COMMENT '邮箱';
    CREATE UNIQUE INDEX uk_email ON user(email);
    ```

    The migration adds an `email` VARCHAR(100) column with a UNIQUE KEY constraint allowing multiple NULL values.
  </action>
  <verify>
    <automated>grep -l "V1.3__add_user_email" backend/src/main/resources/db/migration/V1.3__add_user_email.sql</automated>
  </verify>
  <done>File created at backend/src/main/resources/db/migration/V1.3__add_user_email.sql containing ALTER TABLE and CREATE UNIQUE INDEX for email</done>
  <acceptance_criteria>
    - grep finds "V1.3__add_user_email.sql" in migration directory
    - grep finds "ADD COLUMN email" in the file
    - grep finds "uk_email" in the file
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add email field to User entity</name>
  <files>backend/src/main/java/com/example/iot/entity/User.java</files>
  <read_first>
    - backend/src/main/java/com/example/iot/entity/User.java
  </read_first>
  <action>
    Add the email field to User.java after the `openid` field (line 37) and add getter/setter after the openid getter/setter pair:

    After line 37 (`private String openid;`), add:
    ```java
        @Schema(description = "邮箱")
        private String email;
    ```

    After line 64 (`public void setOpenid(String openid) { this.openid = openid; }`), add:
    ```java
        public String getEmail() { return email; }
        public void setEmail(String email) { this.email = email; }
    ```
  </action>
  <verify>
    <automated>grep -n "email" backend/src/main/java/com/example/iot/entity/User.java | head -20</automated>
  </verify>
  <done>User.java has private String email field, getEmail() and setEmail() methods</done>
  <acceptance_criteria>
    - grep finds "private String email" in User.java
    - grep finds "getEmail()" in User.java
    - grep finds "setEmail(String email)" in User.java
    - grep finds "@Schema(description = \"邮箱\")" in User.java
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add email field to UserRegisterDTO</name>
  <files>backend/src/main/java/com/example/iot/dto/UserRegisterDTO.java</files>
  <read_first>
    - backend/src/main/java/com/example/iot/dto/UserRegisterDTO.java
  </read_first>
  <action>
    Add the email field to UserRegisterDTO.java after the `nickname` field (line 24) and add getter/setter after the nickname getter/setter:

    After line 24 (`private String nickname;`), add:
    ```java
        @Schema(description = "邮箱", example = "user@example.com")
        private String email;
    ```

    After line 31 (`public void setNickname(String nickname) { this.nickname = nickname; }`), add:
    ```java
        public String getEmail() { return email; }
        public void setEmail(String email) { this.email = email; }
    ```
  </action>
  <verify>
    <automated>grep -n "email" backend/src/main/java/com/example/iot/dto/UserRegisterDTO.java | head -20</automated>
  </verify>
  <done>UserRegisterDTO.java has optional email field with getter/setter and @Schema annotation</done>
  <acceptance_criteria>
    - grep finds "private String email" in UserRegisterDTO.java
    - grep finds "getEmail()" in UserRegisterDTO.java
    - grep finds "setEmail(String email)" in UserRegisterDTO.java
    - grep finds '@Schema(description = "邮箱"' in UserRegisterDTO.java
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Add selectByEmail to UserMapper</name>
  <files>backend/src/main/java/com/example/iot/mapper/UserMapper.java</files>
  <read_first>
    - backend/src/main/java/com/example/iot/mapper/UserMapper.java
  </read_first>
  <action>
    Add the selectByEmail method to UserMapper.java following the existing selectByOpenid pattern. Add after line 25 (after selectByOpenid method):

    ```java
        /**
         * 根据邮箱查询用户
         */
        @Select("SELECT * FROM user WHERE email = #{email} AND deleted = 0")
        User selectByEmail(@Param("email") String email);
    ```
  </action>
  <verify>
    <automated>grep -n "selectByEmail" backend/src/main/java/com/example/iot/mapper/UserMapper.java</automated>
  </verify>
  <done>UserMapper.java has selectByEmail method using @Select annotation</done>
  <acceptance_criteria>
    - grep finds "selectByEmail" in UserMapper.java
    - grep finds '@Select("SELECT * FROM user WHERE email = #{email} AND deleted = 0")' in UserMapper.java
    - grep finds "User selectByEmail" in UserMapper.java
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Add EMAIL_ALREADY_EXISTS to ResultCode</name>
  <files>backend/src/main/java/com/example/iot/common/result/ResultCode.java</files>
  <read_first>
    - backend/src/main/java/com/example/iot/common/result/ResultCode.java
  </read_first>
  <action>
    Add EMAIL_ALREADY_EXISTS to ResultCode.java in the auth section (1000-1999 range). After REGISTER_FAILED (line 27, code 1009), add:

    ```java
        EMAIL_ALREADY_EXISTS(1010, "该邮箱已被注册"),
    ```

    The enum line must be added before the closing semicolon of the enum or in the auth block.
  </action>
  <verify>
    <automated>grep -n "EMAIL_ALREADY_EXISTS" backend/src/main/java/com/example/iot/common/result/ResultCode.java</automated>
  </verify>
  <done>ResultCode.java has EMAIL_ALREADY_EXISTS with code 1010 and message "该邮箱已被注册"</done>
  <acceptance_criteria>
    - grep finds "EMAIL_ALREADY_EXISTS" in ResultCode.java
    - grep finds "1010" in ResultCode.java
    - grep finds "该邮箱已被注册" in ResultCode.java
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Update UserServiceImpl.register() with email check and custom messages</name>
  <files>backend/src/main/java/com/example/iot/service/impl/UserServiceImpl.java</files>
  <read_first>
    - backend/src/main/java/com/example/iot/service/impl/UserServiceImpl.java
  </read_first>
  <action>
    Update the register() method in UserServiceImpl.java to:

    1. Change the username conflict exception (line 63-65) to use custom message per D-01:
       ```java
       if (existingUser != null) {
           throw new BusinessException(ResultCode.USER_ALREADY_EXISTS.getCode(),
               "用户名已存在，请尝试其他用户名");
       }
       ```

    2. Add email uniqueness check after the username check (after line 65, before line 67):
       ```java
       if (StringUtils.hasText(dto.getEmail())) {
           User existingEmail = userMapper.selectByEmail(dto.getEmail());
           if (existingEmail != null) {
               throw new BusinessException(ResultCode.EMAIL_ALREADY_EXISTS);
           }
       }
       ```

    3. Add email field to user entity when creating the user (after line 72 `user.setRole("USER");`):
       ```java
       if (StringUtils.hasText(dto.getEmail())) {
           user.setEmail(dto.getEmail());
       }
       ```
  </action>
  <verify>
    <automated>grep -n "用户名已存在，请尝试其他用户名\|selectByEmail\|该邮箱已被注册\|user.setEmail" backend/src/main/java/com/example/iot/service/impl/UserServiceImpl.java</automated>
  </verify>
  <done>UserServiceImpl.register() checks email uniqueness, sets email on User entity, and uses custom messages for both conflict cases</done>
  <acceptance_criteria>
    - grep finds "用户名已存在，请尝试其他用户名" in UserServiceImpl.java
    - grep finds "selectByEmail" in UserServiceImpl.java
    - grep finds "EMAIL_ALREADY_EXISTS" in UserServiceImpl.java
    - grep finds "user.setEmail(dto.getEmail())" in UserServiceImpl.java
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | Registration data (username, password, email) crosses untrusted boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | Registration | mitigate | Email uniqueness check prevents account enumeration via timing differences |
| T-07-02 | Tampering | Database | accept | UNIQUE constraint at DB level is redundant with app check but provides defense-in-depth |
</threat_model>

<verification>
## Phase-Level Verification

After all tasks complete:
1. `grep -rn "用户名已存在，请尝试其他用户名" backend/src/main/java/com/example/iot/service/impl/UserServiceImpl.java` returns the custom message
2. `grep -rn "EMAIL_ALREADY_EXISTS" backend/src/main/java/com/example/iot/common/result/ResultCode.java` returns the new enum value
3. `grep -rn "V1.3__add_user_email" backend/src/main/resources/db/migration/` returns the migration file
4. Manual verification: Start the application and attempt registration with duplicate username and duplicate email to confirm error messages
</verification>

<success_criteria>
1. User registering with existing username receives HTTP 200 with error message "用户名已存在，请尝试其他用户名"
2. User registering with existing email receives HTTP 200 with error message "该邮箱已被注册"
3. User registering with new username and new email succeeds and email is stored in database
4. User registering without email succeeds (email field is optional per D-03)
5. V1.3 migration adds email column with UNIQUE constraint
</success_criteria>

<output>
After completion, create `.planning/phases/07-auth-error/07-01-SUMMARY.md`
</output>
