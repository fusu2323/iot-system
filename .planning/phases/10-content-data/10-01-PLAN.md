---
phase: 10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/resources/db/migration/V1.5__expand_content_data.sql
autonomous: true
requirements:
  - CONTENT-01
  - CONTENT-02
  - CONTENT-03

must_haves:
  truths:
    - "Content table has 24+ entries total (8 new movies, 8 new music, 8 new games)"
    - "All entries use realistic titles from D-01"
    - "Migration is idempotent (can run multiple times safely)"
    - "Existing 8 content entries remain intact"
  artifacts:
    - path: "backend/src/main/resources/db/migration/V1.5__expand_content_data.sql"
      provides: "24 new content entries (8 movies, 8 music, 8 games)"
      contains: "INSERT INTO content"
  key_links:
    - from: "V1.5__expand_content_data.sql"
      to: "content table"
      via: "INSERT ... WHERE NOT EXISTS pattern (per D-03)"
---

<objective>
Expand content table with 24 new entries (8 movies, 8 music, 8 games) using the established idempotent INSERT pattern.

Purpose: Provide realistic sample data for content recommendation feature testing.
Output: V1.5__expand_content_data.sql migration file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/src/main/resources/db/migration/V1.0__create_scene_content_tables.sql
@backend/src/main/resources/db/migration/V1.4__add_scene_mutex_group.sql
@backend/src/main/java/com/example/iot/entity/Content.java

Content table schema:
- id: BIGINT AUTO_INCREMENT
- title: VARCHAR(100)
- type: VARCHAR(20) — MOVIE/MUSIC/GAME
- cover: VARCHAR(255)
- description: TEXT
- rating: DECIMAL(2,1) — 0.0-5.0
- deleted: INT (logical delete)
- create_time: DATETIME
- update_time: DATETIME

Idempotent pattern:
```sql
INSERT INTO `content` (...) SELECT 'value', ... WHERE NOT EXISTS (SELECT 1 FROM `content` WHERE `title` = 'value');
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create V1.5__expand_content_data.sql migration</name>
  <files>backend/src/main/resources/db/migration/V1.5__expand_content_data.sql</files>
  <action>
Create migration file with header comment block indicating Phase 10 purpose and requirements (CONTENT-01, CONTENT-02, CONTENT-03).

Add 24 idempotent INSERT statements following the exact pattern from V1.0:

**Movies (8):** 阿凡达2, 泰坦尼克号, 复仇者联盟, 盗梦空间, 星际穿越, 速度与激情, 变形金刚, 侏罗纪世界

**Music (8):** 泰勒丝 - Anti-Hero, 碧昂丝 - Crazy in Love, 周杰伦 - 夜曲, 林俊杰 - 可惜没如果, 五月天 - 倔强, 迈克尔杰克逊 - Billie Jean, 艾德希兰 - Shape of You, 林肯公园 - Numb

**Games (8):** 使命召唤, 守望先锋, 我的世界, GTA5, 巫师3, 黑暗之魂, 艾尔登法环, 超级马里奥奥德赛

Each INSERT must:
1. Use `INSERT INTO content (...) SELECT ... WHERE NOT EXISTS` pattern
2. Include all columns: title, type, cover, description, rating, create_time, update_time
3. Use realistic descriptions and ratings (3.5-5.0 range)
4. Use cover paths like /covers/movie{N}.jpg pattern
5. Create_time and update_time use NOW()
</action>
  <verify>File exists with 24 INSERT statements, each using WHERE NOT EXISTS pattern</verify>
  <done>Migration file committed with 24 content entries (8 movies, 8 music, 8 games) using idempotent pattern</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB migration | Runs against database; uses idempotent pattern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tamper | V1.5__expand_content_data.sql | mitigate | Idempotent INSERT pattern prevents duplicate data on re-run |
| T-10-02 | Denial | Content table | accept | Data-only change, no schema impact |

<verification>
1. File has correct naming: V1.5__expand_content_data.sql
2. File contains 24 INSERT statements (8 MOVIE, 8 MUSIC, 8 GAME)
3. Each INSERT uses WHERE NOT EXISTS pattern
4. All INSERTs reference correct type values (MOVIE/MUSIC/GAME)
</verification>

<success_criteria>
1. Migration file created at backend/src/main/resources/db/migration/V1.5__expand_content_data.sql
2. File contains exactly 24 new content entries
3. All entries use idempotent INSERT ... WHERE NOT EXISTS pattern
4. Content table will have 32 total entries after migration (8 existing + 24 new)
</criteria>

<output>
After completion, create `.planning/phases/10-content-data/10-01-SUMMARY.md`
</output>