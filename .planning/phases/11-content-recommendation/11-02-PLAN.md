---
phase: 11
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - backend/src/main/java/com/example/iot/service/RecommendationService.java
  - backend/src/main/java/com/example/iot/service/impl/RecommendationServiceImpl.java
  - backend/src/main/java/com/example/iot/controller/RecommendationController.java
autonomous: true
requirements:
  - RECOMMEND-01
  - RECOMMEND-02
user_setup: []
---

<objective>
Update RecommendationService and RecommendationController to support optional type filtering and grouped response structure per D-01 and D-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/src/main/java/com/example/iot/service/RecommendationService.java
@backend/src/main/java/com/example/iot/service/impl/RecommendationServiceImpl.java
@backend/src/main/java/com/example/iot/controller/RecommendationController.java
@backend/src/main/java/com/example/iot/vo/GroupedRecommendationVO.java
@backend/src/main/java/com/example/iot/vo/GroupedRecommendationResponse.java
@backend/src/main/java/com/example/iot/service/impl/ContentServiceImpl.java
</context>

<interfaces>
RecommendationService interface signature changes:
- Old: IPage<RecommendationVO> getRecommendations(Long userId, Integer page, Integer size)
- New: GroupedRecommendationResponse getRecommendations(Long userId, String type, Integer page, Integer size)

GroupedRecommendationResponse and GroupedRecommendationVO are created in plan 11-01 and available as dependencies.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update RecommendationService interface signature</name>
  <files>backend/src/main/java/com/example/iot/service/RecommendationService.java</files>
  <read_first>
    backend/src/main/java/com/example/iot/service/RecommendationService.java
  </read_first>
  <action>
    Update the getRecommendations method signature in RecommendationService.java:
    - Change return type from IPage&lt;RecommendationVO&gt; to GroupedRecommendationResponse
    - Add parameter: String type (optional type filter)
    - Update @param Javadoc to document new type parameter

    The new signature should be:
    GroupedRecommendationResponse getRecommendations(Long userId, String type, Integer page, Integer size);
  </action>
  <verify>
    <automated>grep -c "GroupedRecommendationResponse" backend/src/main/java/com/example/iot/service/RecommendationService.java</automated>
  </verify>
  <done>
    RecommendationService.java has updated getRecommendations signature returning GroupedRecommendationResponse with String type parameter
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement type filtering and grouped response in RecommendationServiceImpl</name>
  <files>backend/src/main/java/com/example/iot/service/impl/RecommendationServiceImpl.java</files>
  <read_first>
    backend/src/main/java/com/example/iot/service/impl/RecommendationServiceImpl.java
    backend/src/main/java/com/example/iot/service/impl/ContentServiceImpl.java
  </read_first>
  <action>
    Modify RecommendationServiceImpl.java to implement type filtering and grouped response:

    1. Update getRecommendations method signature to match interface (String type parameter)

    2. Add type filtering in step 3 (获取所有内容): Use the same pattern as ContentServiceImpl.list() - wrap content query in if (StringUtils.hasText(type)) { filter by type }

    3. After step 4 (计算每个内容的推荐分数), implement grouping logic:
       - Use Collectors.groupingBy() to group contentScores by content.getType()
       - Ensure all three types (MOVIE, MUSIC, GAME) are present in the result Map, even if empty
       - Sort each group by score descending
       - Apply pagination per group using page and size parameters
       - Build GroupedRecommendationResponse with Map&lt;String, GroupedRecommendationVO&gt;

    4. Remove hardcoded CLICK_WEIGHT, LIKE_WEIGHT, DISLIKE_WEIGHT constants (lines 41-43) - they will be replaced by configurable weights in plan 11-03

    5. Import GroupedRecommendationVO and GroupedRecommendationResponse

    Use LinkedHashMap to maintain insertion order and ensure MOVIE, MUSIC, GAME appear in consistent order.
  </action>
  <verify>
    <automated>grep -c "GroupedRecommendationResponse" backend/src/main/java/com/example/iot/service/impl/RecommendationServiceImpl.java</automated>
  </verify>
  <done>
    RecommendationServiceImpl.getRecommendations() returns GroupedRecommendationResponse, filters by type when provided, groups results by content type, sorts each group by score
  </done>
</task>

<task type="auto">
  <name>Task 3: Update RecommendationController to add type parameter and change return type</name>
  <files>backend/src/main/java/com/example/iot/controller/RecommendationController.java</files>
  <read_first>
    backend/src/main/java/com/example/iot/controller/RecommendationController.java
  </read_first>
  <action>
    Modify RecommendationController.getRecommendations() method:
    1. Add @RequestParam(required = false) String type parameter after userId
    2. Add @Parameter(description = "内容类型筛选", example = "MOVIE") annotation
    3. Change return type from Result&lt;IPage&lt;RecommendationVO&gt;&gt; to Result&lt;GroupedRecommendationResponse&gt;
    4. Update the method call to pass type parameter: recommendationService.getRecommendations(userId, type, page, size)
    5. Update @Operation summary to reflect grouped response

    Keep the existing page and size parameters with defaults.
  </action>
  <verify>
    <automated>grep -c "GroupedRecommendationResponse" backend/src/main/java/com/example/iot/controller/RecommendationController.java</automated>
  </verify>
  <done>
    RecommendationController.getRecommendations() accepts optional type parameter and returns GroupedRecommendationResponse
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Untrusted input: userId, type, page, size |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | type parameter | accept | Type validation happens via ContentServiceImpl.list() pattern; invalid types return empty results |
| T-11-04 | Information Disclosure | grouped response | accept | Response structure is documented via @Schema annotations |
</threat_model>

<verification>
- RecommendationService.java interface updated with new signature
- RecommendationServiceImpl.getRecommendations() implements type filtering and grouped response
- RecommendationController.getRecommendations() accepts optional type param and returns GroupedRecommendationResponse
- Build compiles: mvn compile -q (if available) or manual inspection confirms no syntax errors
</verification>

<success_criteria>
- GET /api/recommendations?userId=1 returns grouped recommendations with all content types
- GET /api/recommendations?userId=1&type=MOVIE returns only MOVIE recommendations
- Each group contains type, items (list), and total count
- Results grouped as { groups: { MOVIE: {...}, MUSIC: {...}, GAME: {...} }, total: N }
</success_criteria>

<output>
After completion, create `.planning/phases/11-content-recommendation/11-02-SUMMARY.md`
</output>
