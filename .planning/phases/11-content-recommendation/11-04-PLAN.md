---
phase: 11
plan: 04
type: execute
wave: 3
depends_on:
  - 11-01
files_modified:
  - backend/src/main/java/com/example/iot/controller/ContentController.java
  - backend/src/main/java/com/example/iot/service/impl/ContentServiceImpl.java
autonomous: true
requirements:
  - CONTENT-04
user_setup: []
---

<objective>
Verify and document that ContentController.list() and ContentServiceImpl.list() support optional type filtering as required by CONTENT-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/src/main/java/com/example/iot/controller/ContentController.java
@backend/src/main/java/com/example/iot/service/impl/ContentServiceImpl.java
</context>

<interfaces>
ContentController.list() method signature:
- Current: Result<IPage<ContentVO>> list(Integer page, Integer size, String keyword, String type)
- The type parameter is @RequestParam(required = false) String type

ContentServiceImpl.list() method signature:
- Current: IPage<ContentVO> list(Integer page, Integer size, String keyword, String type)
- Type filtering: if (StringUtils.hasText(type)) { wrapper.eq(Content::getType, type); }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify type filtering implementation on ContentController.list()</name>
  <files>backend/src/main/java/com/example/iot/controller/ContentController.java</files>
  <read_first>
    backend/src/main/java/com/example/iot/controller/ContentController.java
  </read_first>
  <action>
    Verify ContentController.list() method has:
    1. @Parameter(description = "内容类型", example = "MOVIE") annotation on type parameter
    2. @RequestParam(required = false) String type parameter
    3. Passes type to contentService.list(page, size, keyword, type)

    If any of these are missing, add them following the pattern in the read_first file.
  </action>
  <verify>
    <automated>grep -c "required = false.*String type" backend/src/main/java/com/example/iot/controller/ContentController.java</automated>
  </verify>
  <done>
    ContentController.list() accepts optional type parameter and passes it to ContentService.list()
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify type filtering implementation on ContentServiceImpl.list()</name>
  <files>backend/src/main/java/com/example/iot/service/impl/ContentServiceImpl.java</files>
  <read_first>
    backend/src/main/java/com/example/iot/service/impl/ContentServiceImpl.java
  </read_first>
  <action>
    Verify ContentServiceImpl.list() method implements type filtering:
    1. Has String type parameter
    2. Contains: if (StringUtils.hasText(type)) { wrapper.eq(Content::getType, type); }

    If the filtering logic is missing, add it following the pattern in the read_first file.
  </action>
  <verify>
    <automated>grep -c "StringUtils.hasText.type" backend/src/main/java/com/example/iot/service/impl/ContentServiceImpl.java</automated>
  </verify>
  <done>
    ContentServiceImpl.list() filters content by type when the type parameter is provided
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Untrusted input: type parameter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Tampering | type parameter | accept | Invalid type values return empty results, not errors |
| T-11-07 | Information Disclosure | type filter | accept | Filter is on user-provided input, no sensitive data exposed |
</threat_model>

<verification>
- ContentController.list() has @RequestParam(required = false) String type
- ContentServiceImpl.list() has type filtering via StringUtils.hasText check
- GET /api/contents?type=MOVIE returns only MOVIE content
- GET /api/contents returns all content types
</verification>

<success_criteria>
- GET /api/contents?type=MOVIE returns only content where type="MOVIE"
- GET /api/contents?type=MUSIC returns only content where type="MUSIC"
- GET /api/contents?type=GAME returns only content where type="GAME"
- GET /api/contents (no type) returns all content
- Type filtering works in combination with keyword and pagination
</success_criteria>

<output>
After completion, create `.planning/phases/11-content-recommendation/11-04-SUMMARY.md`
</output>
