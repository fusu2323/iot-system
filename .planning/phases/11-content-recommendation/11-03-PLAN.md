---
phase: 11
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - backend/src/main/java/com/example/iot/service/UserPreferenceService.java
  - backend/src/main/java/com/example/iot/service/impl/UserPreferenceServiceImpl.java
  - backend/src/main/java/com/example/iot/controller/PreferenceController.java
autonomous: true
requirements:
  - RECOMMEND-03
user_setup: []
---

<objective>
Extend UserPreferenceService and PreferenceController to support weight configuration (clickWeight, likeWeight, dislikeWeight) per D-03 and D-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/src/main/java/com/example/iot/service/UserPreferenceService.java
@backend/src/main/java/com/example/iot/service/impl/UserPreferenceServiceImpl.java
@backend/src/main/java/com/example/iot/controller/PreferenceController.java
@backend/src/main/java/com/example/iot/entity/UserPreference.java
@backend/src/main/java/com/example/iot/vo/UserPreferenceVO.java
@backend/src/main/java/com/example/iot/dto/UserPreferenceDTO.java
</context>

<interfaces>
UserPreferenceService interface changes:
- New method signature: void setPreference(Long userId, String contentType, Integer score, Integer clickWeight, Integer likeWeight, Integer dislikeWeight)
- Existing setPreference(userId, contentType, score) remains for backward compatibility

PreferenceController - POST /api/preferences already exists, it will call the new 4-param setPreference method when weight fields are provided in DTO.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add weighted setPreference method to UserPreferenceService interface</name>
  <files>backend/src/main/java/com/example/iot/service/UserPreferenceService.java</files>
  <read_first>
    backend/src/main/java/com/example/iot/service/UserPreferenceService.java
  </read_first>
  <action>
    Add a new overloaded method signature to UserPreferenceService.java:
    /**
     * 设置用户偏好（包含权重配置）
     *
     * @param userId 用户 ID
     * @param contentType 内容类型
     * @param score 偏好分数
     * @param clickWeight 点击行为权重 (nullable, defaults to 5)
     * @param likeWeight 收藏行为权重 (nullable, defaults to 10)
     * @param dislikeWeight 不喜欢行为权重 (nullable, defaults to -20)
     */
    void setPreference(Long userId, String contentType, Integer score, Integer clickWeight, Integer likeWeight, Integer dislikeWeight);

    Keep the existing 3-param setPreference for backward compatibility.
  </action>
  <verify>
    <automated>grep -c "setPreference.*clickWeight" backend/src/main/java/com/example/iot/service/UserPreferenceService.java</automated>
  </verify>
  <done>
    UserPreferenceService.java has new 6-param setPreference method accepting weight parameters
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement weighted setPreference in UserPreferenceServiceImpl</name>
  <files>backend/src/main/java/com/example/iot/service/impl/UserPreferenceServiceImpl.java</files>
  <read_first>
    backend/src/main/java/com/example/iot/service/impl/UserPreferenceServiceImpl.java
    backend/src/main/java/com/example/iot/entity/UserPreference.java
  </read_first>
  <action>
    Implement the new 6-param setPreference method in UserPreferenceServiceImpl.java:

    1. Query existing preference via selectByUserIdAndContentType
    2. If exists: update preferenceScore, clickWeight, likeWeight, dislikeWeight fields
    3. If new: create new UserPreference with all fields, using defaults (5/10/-20) when weights are null
    4. Use userPreferenceMapper.updateById or insert

    Also update convertToVO method to map the three weight fields to UserPreferenceVO.

    Ensure null-safe handling: when weight params are null, use defaults.
  </action>
  <verify>
    <automated>grep -c "clickWeight" backend/src/main/java/com/example/iot/service/impl/UserPreferenceServiceImpl.java</automated>
  </verify>
  <done>
    UserPreferenceServiceImpl.setPreference() with weights implemented, convertToVO maps weight fields
  </done>
</task>

<task type="auto">
  <name>Task 3: Update PreferenceController to pass weights to service</name>
  <files>backend/src/main/java/com/example/iot/controller/PreferenceController.java</files>
  <read_first>
    backend/src/main/java/com/example/iot/controller/PreferenceController.java
  </read_first>
  <action>
    Modify PreferenceController.setPreference() to call the new weighted service method:

    Change the service call from:
    userPreferenceService.setPreference(userId, dto.getContentType(), dto.getPreferenceScore());

    To:
    userPreferenceService.setPreference(userId, dto.getContentType(), dto.getPreferenceScore(),
        dto.getClickWeight(), dto.getLikeWeight(), dto.getDislikeWeight());

    The DTO now has weight fields (added in plan 11-01), so they will be passed through.
  </action>
  <verify>
    <automated>grep -c "setPreference" backend/src/main/java/com/example/iot/controller/PreferenceController.java</automated>
  </verify>
  <done>
    PreferenceController.setPreference() passes weight fields from DTO to service
  </done>
</task>

<task type="auto">
  <name>Task 4: Update RecommendationServiceImpl to use configurable weights</name>
  <files>backend/src/main/java/com/example/iot/service/impl/RecommendationServiceImpl.java</files>
  <read_first>
    backend/src/main/java/com/example/iot/service/impl/RecommendationServiceImpl.java
  </read_first>
  <action>
    Update RecommendationServiceImpl to use configurable weights from UserPreference instead of hardcoded constants:

    1. Remove the hardcoded constants (CLICK_WEIGHT = 5, LIKE_WEIGHT = 10, DISLIKE_WEIGHT = -20) - plan 11-02 already removed these

    2. Modify recordClick(), recordLike(), recordDislike() methods to fetch weights from UserPreference:
       - After getting Content, call userPreferenceMapper.selectByUserIdAndContentType(userId, content.getType())
       - Use pref.getClickWeight() / pref.getLikeWeight() / pref.getDislikeWeight() if not null, otherwise use defaults (5/10/-20)
       - Pass the fetched weight to updateUserPreferenceScore()

    3. Modify getFeedbackWeight() method to accept contentType parameter and fetch from UserPreference:
       - Lookup UserPreference via userPreferenceMapper.selectByUserIdAndContentType(userId, contentType)
       - Return configurable weight or default if null

    4. Modify updateUserPreferenceScore() to set default weights when creating new preference:
       - When inserting new UserPreference, set clickWeight=5, likeWeight=10, dislikeWeight=-20
  </action>
  <verify>
    <automated>grep -c "getClickWeight\|getLikeWeight\|getDislikeWeight" backend/src/main/java/com/example/iot/service/impl/RecommendationServiceImpl.java</automated>
  </verify>
  <done>
    RecommendationServiceImpl uses configurable weights from UserPreference table instead of hardcoded constants
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Untrusted input via UserPreferenceDTO (clickWeight, likeWeight, dislikeWeight) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering | weight values | mitigate | Validate weight ranges: clickWeight and likeWeight should be >=0 and <=100, dislikeWeight should be >=-100 and <=0. Log warning for out-of-range values but use boundary values. |
| T-11-06 | Denial of Service | null weights | mitigate | Null-safe access with defaults (5/10/-20) when weights are not set |
</threat_model>

<verification>
- UserPreferenceService.java has new 6-param setPreference method
- UserPreferenceServiceImpl implements weighted setPreference with null-safe defaults
- PreferenceController passes weight fields from DTO to service
- RecommendationServiceImpl uses configurable weights from UserPreference
- Build compiles successfully
</verification>

<success_criteria>
- POST /api/preferences with weight fields in JSON body sets custom weights for that content type
- When weights are null, defaults (5/10/-20) are used
- recordClick/recordLike/recordDislike use configurable weights from user_preference table
- Existing user preferences with NULL weights fall back to defaults (5/10/-20)
</success_criteria>

<output>
After completion, create `.planning/phases/11-content-recommendation/11-03-SUMMARY.md`
</output>
