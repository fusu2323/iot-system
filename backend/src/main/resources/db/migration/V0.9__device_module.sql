-- =====================================================
-- 数据库变更脚本 - Phase 3: 设备模块
-- =====================================================
-- 变更时间：2026-03-12
-- 变更说明：创建设备表及设备统计相关功能
-- 前置版本：v0.5 (Phase 1-2)
-- 当前版本：v0.9 (Phase 3)
-- =====================================================

USE `iot_system`;

-- =============================================
-- 说明：Phase 3 的表结构变更
-- =============================================

-- 设备表 (device) - Phase 3
-- 已在 init.sql 中定义，此处仅做留痕记录

-- =============================================
-- 索引优化
-- =============================================

-- 为设备表添加类型索引（如果不存在）
ALTER TABLE `device` ADD INDEX IF NOT EXISTS `idx_type` (`type`) COMMENT '设备类型索引';
ALTER TABLE `device` ADD INDEX IF NOT EXISTS `idx_room` (`room`) COMMENT '房间索引';
ALTER TABLE `device` ADD INDEX IF NOT EXISTS `idx_status` (`status`) COMMENT '设备状态索引';

-- =============================================
-- 变更完成
-- =============================================
